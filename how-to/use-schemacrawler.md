# Use schemacrawler

```shell
sh schemacrawler.sh \
  -c=graph \
  -infolevel=standard \
  -server=sqlite \
  -database=/path-to/file.sqlite \
  -outputformat=pdf \
  -outputfile=/path-to/output-db.pdf
```
